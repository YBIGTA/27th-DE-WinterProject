apiVersion: v1
kind: Service
metadata:
  name: ingestor
  namespace: taxi-pipeline
  labels:
    app: ingestor
spec:
  type: NodePort
  selector:
    app: ingestor
  ports:
    - name: http
      port: 8080
      targetPort: http
      nodePort: 30080
