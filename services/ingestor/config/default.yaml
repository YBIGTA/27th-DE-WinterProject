# Ingestor runtime config (non-network)
app:
  kafka:
    topic: taxi-event-data
  tuning:
    buffer:
      size: 10000
    batch:
      size: 500
      timeout_ms: 50
    kafka:
      send_concurrency: 4
    metrics:
      interval_sec: 10
